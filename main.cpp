#include "raylib.h"
#include <string>
int main() {

    int initWidth = 350;
    int initHeight = 200;
    const char* initTitleText = "Simple Windows";

    InitWindow(initWidth, initHeight, initTitleText);
}
